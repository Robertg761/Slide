<?xml version="1.0" encoding="utf-8"?>
<!--
  Backup and device-transfer rules for Android 12 and above.

  The learned words and learned pairs are excluded from both. See backup_rules.xml for why; the
  reasoning applies as much to a transfer to a new phone as to a cloud backup, since neither is a
  place the user asked their personal vocabulary to go. The legacy settings DataStore is excluded
  because releases before 0.2.1 stored recent emoji usage there; current releases migrate that
  history into noBackupFilesDir.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="learned_words.txt" />
        <exclude domain="file" path="learned_words.txt.tmp" />
        <exclude domain="file" path="learned_pairs.txt" />
        <exclude domain="file" path="learned_pairs.txt.tmp" />
        <exclude domain="file" path="datastore/slide_settings.preferences_pb" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="learned_words.txt" />
        <exclude domain="file" path="learned_words.txt.tmp" />
        <exclude domain="file" path="learned_pairs.txt" />
        <exclude domain="file" path="learned_pairs.txt.tmp" />
        <exclude domain="file" path="datastore/slide_settings.preferences_pb" />
    </device-transfer>
</data-extraction-rules>
